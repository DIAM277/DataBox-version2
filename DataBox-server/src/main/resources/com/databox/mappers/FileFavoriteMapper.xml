<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.databox.mappers.FileFavoriteMapper">

    <select id="selectByUserIdAndFileId" resultType="com.databox.entity.po.FileFavorite">
        SELECT favorite_id as favoriteId, user_id as userId, file_id as fileId, create_time as createTime
        FROM file_favorite
        WHERE user_id = #{userId} AND file_id = #{fileId}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.databox.entity.po.FileFavorite">
        INSERT INTO file_favorite(user_id, file_id, create_time)
        VALUES(#{userId}, #{fileId}, #{createTime})
    </insert>

    <delete id="deleteByUserIdAndFileId">
        DELETE FROM file_favorite
        WHERE user_id = #{userId} AND file_id = #{fileId}
    </delete>

</mapper>